name: Deploy Azure SQL Database schema

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  
  deploy_database_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: windows-latest
    name: Deploy Database Job
    steps:
      - uses: actions/checkout@v1
      - uses: Azure/sql-action@v1
        with:
          server-name: bus-server840307.database.windows.net
          connection-string: Server=tcp:bus-server840307.database.windows.net,1433;Database=bus-db;User ID=cloudadmin;Password=Wedgebem01!;Encrypt=true;Connection Timeout=30;
          # uses dacpac to deploy database schema
          dacpac-package: './Database/dacpac/bus-db.dacpac'